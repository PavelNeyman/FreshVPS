# Bootstrap

```bash
curl -fsSL https://raw.githubusercontent.com/PavelNeyman/netductor/main/bootstrap.sh | bash
```

Ставит `/usr/local/bin/netductor` из релиза `v0.7.0-dev` (переменная `NETDUCTOR_VERSION`).
